/**
 * The attribute a report's own data travels under, so that it never meets
 * the application's global attributes of the same name.
 */
export const FLARE_ATTRIBUTE = "flare";

/** How Datadog classes an error the application reported itself. */
export const CUSTOM_SOURCE = "CUSTOM";
