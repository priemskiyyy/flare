export const DEFAULT_BUFFER = { maxReports: 30, maxAgeMs: 60_000 };
export const DEFAULT_DEADLINE_MS = 5_000;
export const DEFAULT_DEDUPE = { windowMs: 1_000, maxKeys: 100 };
export const DEFAULT_REPORTS_PER_MINUTE = 120;
export const DEFAULT_FLUSH_TIMEOUT_MS = 2_000;
